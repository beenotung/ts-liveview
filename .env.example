# Environment Configuration
# Copy this file to .env and fill in the values

# Application Environment
# Set to 'production' for live deployment, 'development' for local development
NODE_ENV=development

# HTTPS Proxy (for development with camera/microphone access)
# Set to 'enable' to automatically start Caddy HTTPS reverse proxy
# Required for accessing camera/microphone in browser during development
CADDY_PROXY=skip

# Server Configuration
# Port number for the server to listen on
PORT=8100
# Base URL of the application (auto-configured in development mode)
ORIGIN=http://localhost:8100
# Secret key for cookie signing (auto-generates random UUID if not set)
COOKIE_SECRET=

# Upload Directory
# Directory path for file uploads relative to project root
UPLOAD_DIR=uploads

# Analytics
# API key for findip.net geolocation service (set to 'skip' to disable)
FIND_IP_API_KEY=skip
